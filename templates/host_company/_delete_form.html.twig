<form method="post" action="{{ path('app_host_company_delete', {'id': host_company.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ host_company.id) }}">
    <button class="btn">Delete</button>
</form>
