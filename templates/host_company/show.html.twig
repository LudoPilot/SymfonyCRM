{% extends 'base.html.twig' %}

{% block title %}HostCompany{% endblock %}

{% block body %}
    <h1>HostCompany</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ host_company.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ host_company.name }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ host_company.phone }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ host_company.address }}</td>
            </tr>
            <tr>
                <th>Zipcode</th>
                <td>{{ host_company.zipcode }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ host_company.city }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ host_company.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_host_company_index') }}">back to list</a>

    <a href="{{ path('app_host_company_edit', {'id': host_company.id}) }}">edit</a>

    {{ include('host_company/_delete_form.html.twig') }}
{% endblock %}
