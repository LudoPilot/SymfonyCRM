{{ form_start(form, {'action': event.id ? path('app_event_edit', {'id': event.id}) : path('app_event_new')}) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
